<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AddressMapper">
    <resultMap id="BaseResultMap" type="com.ruoyi.system.domain.Address">
        <!--@Table address-->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="address" jdbcType="VARCHAR" property="address"/>
        <result column="is_del" jdbcType="CHAR" property="isDel"/>
        <result column="is_default" jdbcType="CHAR" property="isDefault"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>
    <sql id="Base_Column_List">
        id,
        user_id,
        phone,
        name,
        address,
        is_default,
        is_del,
        create_time
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from address
        where id = #{id} and is_del = 0
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.Address">
        insert into address (user_id,
                             phone, name, address,
                             create_time)
        values (#{userId},
                #{phone,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{address,jdbcType=VARCHAR},
                now())
    </insert>

    <insert id="insertSelective" parameterType="com.ruoyi.system.domain.Address">
        insert into address
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            <if test="phone != null and phone != ''">
                phone,
            </if>
            <if test="name != null and name != ''">
                name,
            </if>
            <if test="address != null and address != ''">
                address,
            </if>
            <if test="isDel != null and isDel != ''">
                is_del,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=VARCHAR},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            <if test="phone != null and phone != ''">
                #{phone,jdbcType=VARCHAR},
            </if>
            <if test="name != null and name != ''">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="address != null and address != ''">
                #{address,jdbcType=VARCHAR},
            </if>
            <if test="isDel != null and isDel != ''">
                #{isDel,jdbcType=CHAR},
            </if>
            <if test="createTime != null">
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.ruoyi.system.domain.Address">
        update address
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone,jdbcType=VARCHAR},
            </if>
            <if test="name != null and name != ''">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="address != null and address != ''">
                address = #{address,jdbcType=VARCHAR},
            </if>
            <if test="isDel != null and isDel != ''">
                is_del = #{isDel,jdbcType=CHAR},
            </if>
            <if test="createTime != null">
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.ruoyi.system.domain.Address">
        update address
        set phone    = #{phone,jdbcType=VARCHAR},
            name = #{name,jdbcType=VARCHAR},
            address  = #{address,jdbcType=VARCHAR}
        where id = #{id}
    </update>

    <update id="deleteAddressById">
        update address
        set is_del=1
        where id = #{id}
    </update>

    <select id="getAddressList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from address
        where is_del = 0 and user_id=#{userId}
    </select>

    <select id="selectAddressesByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from address
        where is_del = 0
          and user_id = #{userId}
    </select>

    <update id="updateDefaultAddressById">
        update address
        set is_default = 1
        where id = #{id}
    </update>

    <update id="updateNoDefaultAddressByIds">
        update address
        set is_default = 0 where is_del = 0
                             and id in
        <foreach collection="ids" item="key" index="index" open="(" close=")" separator=",">
            #{key}
        </foreach>
    </update>

    <select id="getAddressesByUserIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from address
        where is_del = 0
          and user_id in
        <foreach collection="userIds" item="key" index="index" open="(" close=")" separator=",">
            #{key}
        </foreach>
    </select>
</mapper>